import { ClientScheduleOptimizer } from './planner';

/**
 * Export the ClientScheduleOptimizer as the main export
 * This is just a forwarder to the new modular implementation
 */
export { ClientScheduleOptimizer };

/**
 * Export helper functions
 */
export { fetchFilteredCamps } from './fetchUtils';